# Пересоздаётся switch_theme.sh при каждой смене обоев.
# Пустой monitor — «на всех экранах», включая подключённые позже.
wallpaper {
    monitor =
    path = ~/.config/hypr/wallpaper/ember_stripes.jpg
    fit_mode = cover
}
splash = false
ipc = on
