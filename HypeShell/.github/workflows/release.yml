name: Release

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to release (e.g., v1.0.1)"
        required: true
        type: string

permissions:
  contents: write
  actions: write

concurrency:
  group: release-${{ inputs.tag }}
  cancel-in-progress: true

jobs:
  build-core:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        arch: [amd64, arm64]

    defaults:
      run:
        working-directory: core

    env:
      TAG: ${{ inputs.tag }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ inputs.tag }}
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: ./core/go.mod

      - name: Format check
        run: |
          if [ "$(gofmt -s -l . | wc -l)" -gt 0 ]; then
            echo "The following files are not formatted:"
            gofmt -s -l .
            exit 1
          fi

      - name: Run tests
        run: go test -v ./...

      - name: Build hypeinstall (${{ matrix.arch }})
        env:
          GOOS: linux
          CGO_ENABLED: 0
          GOARCH: ${{ matrix.arch }}
        run: |
          set -eux
          cd cmd/hypeinstall
          go build -trimpath -ldflags "-s -w -X main.Version=${TAG}" \
            -o ../../hypeinstall-${{ matrix.arch }}
          cd ../..
          gzip -9 -k hypeinstall-${{ matrix.arch }}
          sha256sum hypeinstall-${{ matrix.arch }}.gz > hypeinstall-${{ matrix.arch }}.gz.sha256

      - name: Build hype (${{ matrix.arch }})
        env:
          GOOS: linux
          CGO_ENABLED: 0
          GOARCH: ${{ matrix.arch }}
        run: |
          set -eux
          cd cmd/hype
          go build -trimpath -ldflags "-s -w -X main.Version=${TAG}" \
            -o ../../hype-${{ matrix.arch }}
          cd ../..
          gzip -9 -k hype-${{ matrix.arch }}
          sha256sum hype-${{ matrix.arch }}.gz > hype-${{ matrix.arch }}.gz.sha256

      - name: Generate shell completions
        if: matrix.arch == 'amd64'
        run: |
          set -eux
          chmod +x hype-amd64
          ./hype-amd64 completion bash > completion.bash
          ./hype-amd64 completion fish > completion.fish
          ./hype-amd64 completion zsh > completion.zsh

      - name: Build hype-distropkg (${{ matrix.arch }})
        env:
          GOOS: linux
          CGO_ENABLED: 0
          GOARCH: ${{ matrix.arch }}
        run: |
          set -eux
          cd cmd/hype
          go build -trimpath -tags distro_binary -ldflags "-s -w -X main.Version=${TAG}" \
            -o ../../hype-distropkg-${{ matrix.arch }}
          cd ../..
          gzip -9 -k hype-distropkg-${{ matrix.arch }}
          sha256sum hype-distropkg-${{ matrix.arch }}.gz > hype-distropkg-${{ matrix.arch }}.gz.sha256

      - name: Upload artifacts (${{ matrix.arch }})
        if: matrix.arch == 'arm64'
        uses: actions/upload-artifact@v5
        with:
          name: core-assets-${{ matrix.arch }}
          path: |
            core/hypeinstall-${{ matrix.arch }}.gz
            core/hypeinstall-${{ matrix.arch }}.gz.sha256
            core/hype-${{ matrix.arch }}.gz
            core/hype-${{ matrix.arch }}.gz.sha256
            core/hype-distropkg-${{ matrix.arch }}.gz
            core/hype-distropkg-${{ matrix.arch }}.gz.sha256
          if-no-files-found: error

      - name: Upload artifacts with completions
        if: matrix.arch == 'amd64'
        uses: actions/upload-artifact@v5
        with:
          name: core-assets-${{ matrix.arch }}
          path: |
            core/hypeinstall-${{ matrix.arch }}.gz
            core/hypeinstall-${{ matrix.arch }}.gz.sha256
            core/hype-${{ matrix.arch }}.gz
            core/hype-${{ matrix.arch }}.gz.sha256
            core/hype-distropkg-${{ matrix.arch }}.gz
            core/hype-distropkg-${{ matrix.arch }}.gz.sha256
            core/completion.bash
            core/completion.fish
            core/completion.zsh
          if-no-files-found: error

  # update-versions:
  #   runs-on: ubuntu-latest
  #   needs: build-core
  #   steps:
  #     - name: Create GitHub App token
  #       id: app_token
  #       uses: actions/create-github-app-token@v1
  #       with:
  #         app-id: ${{ secrets.APP_ID }}
  #         private-key: ${{ secrets.APP_PRIVATE_KEY }}

  #     - name: Checkout
  #       uses: actions/checkout@v6
  #       with:
  #         token: ${{ steps.app_token.outputs.token }}
  #         fetch-depth: 0

  #     - name: Update VERSION
  #       env:
  #         GH_TOKEN: ${{ steps.app_token.outputs.token }}
  #       run: |
  #         set -euo pipefail
  #         git config user.name "hype-ci[bot]"
  #         git config user.email "hype-ci[bot]@users.noreply.github.com"

  #         version="${GITHUB_REF#refs/tags/}"
  #         echo "Updating to version: $version"
  #         echo "${version}" > quickshell/VERSION
  #         git add quickshell/VERSION

  #         if ! git diff --cached --quiet; then
  #           git commit -m "chore: bump version to $version"
  #           git pull --rebase origin master
  #           git push https://x-access-token:${GH_TOKEN}@github.com/${{ github.repository }}.git HEAD:master
  #         fi

  #         git tag -f "${version}"
  #         git push -f https://x-access-token:${GH_TOKEN}@github.com/${{ github.repository }}.git "${version}"

  release:
    runs-on: ubuntu-24.04
    needs: [build-core] #, update-versions]
    env:
      TAG: ${{ inputs.tag }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ inputs.tag }}
          fetch-depth: 0

      - name: Fetch updated tag after version bump
        run: |
          git fetch origin --force tag ${TAG}
          git checkout ${TAG}

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: ./core/go.mod

      - name: Download core artifacts
        uses: actions/download-artifact@v5
        with:
          pattern: core-assets-*
          merge-multiple: true
          path: ./_core_assets

      - name: Generate Changelog
        id: changelog
        run: |
          set -e
          PREVIOUS_TAG=$(git describe --tags --abbrev=0 "${TAG}^" 2>/dev/null || echo "")
          if [ -z "$PREVIOUS_TAG" ]; then
            CHANGELOG=$(git log --oneline --pretty=format:"%an|%s (%h)" | grep -v "^github-actions\[bot\]|" | sed 's/^[^|]*|/- /' | head -50)
          else
            CHANGELOG=$(git log --oneline --pretty=format:"%an|%s (%h)" "${PREVIOUS_TAG}..${TAG}" | grep -v "^github-actions\[bot\]|" | sed 's/^[^|]*|/- /')
          fi

          cat > RELEASE_BODY.md << 'EOF'
          ## Installation

          ```bash
          curl -fsSL https://install.hypelinux.com | sh
          ```

          ## Assets

          ### Complete Packages
          - **`hype-full-amd64.tar.gz`** - Complete package for x86_64 systems (CLI binaries + QML source + shell completions + installation guide)
          - **`hype-full-arm64.tar.gz`** - Complete package for ARM64 systems (CLI binaries + QML source + shell completions + installation guide)

          ### Individual Components
          - **`hype-cli-amd64.gz`** - HYPE CLI binary for x86_64 systems
          - **`hype-cli-arm64.gz`** - HYPE CLI binary for ARM64 systems
          - **`hype-distropkg-amd64.gz`** - HYPE CLI binary built with distro_package tag for AMD64 systems
          - **`hype-distropkg-arm64.gz`** - HYPE CLI binary built with distro_package tag for ARM64 systems
          - **`hypeinstall-amd64.gz`** - Installer binary for x86_64 systems
          - **`hypeinstall-arm64.gz`** - Installer binary for ARM64 systems
          - **`hype-cli-<version>.tar.gz`** - Go source code with vendored modules (for distro packaging)
          - **`hype-qml.tar.gz`** - QML source code only

          ### Checksums
          - **`*.sha256`** - SHA256 checksums for verifying download integrity

          **Installation:** Extract the `hype-full-*.tar.gz` package for your architecture and follow the `INSTALL.md` instructions inside.

          ---

          EOF

          cat >> RELEASE_BODY.md << EOF
          ## What's Changed

          $CHANGELOG

          **Full Changelog**: https://github.com/${{ github.repository }}/compare/${PREVIOUS_TAG}...${TAG}
          EOF

          echo "changelog<<EOF" >> $GITHUB_OUTPUT
          cat RELEASE_BODY.md >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Prepare release assets
        run: |
          set -euxo pipefail

          mkdir -p _release_assets

          # Copy core binaries and rename hype-*.gz to hype-cli-*.gz
          for file in _core_assets/hype-*.gz*; do
            if [ -f "$file" ]; then
              basename=$(basename "$file")
              if [[ "$basename" == hype-distropkg-* ]]; then
                cp "$file" "_release_assets/$basename"
              else
                newname=$(echo "$basename" | sed 's/^hype-/hype-cli-/')
                cp "$file" "_release_assets/$newname"
              fi
            fi
          done

          # Copy hypeinstall binaries
          cp _core_assets/hypeinstall-*.gz* _release_assets/

          # Copy completions
          cp _core_assets/completion.* _release_assets/ 2>/dev/null || true

          # Create QML source package (exclude build artifacts and git files)
          # Copy root LICENSE and CONTRIBUTING.md to quickshell/ for packaging
          cp LICENSE CONTRIBUTING.md quickshell/

          # Copy root assets directory to quickshell for systemd service and desktop file
          cp -r assets quickshell/

          # Tar the CONTENTS of quickshell/, not the directory itself
          (cd quickshell && tar --exclude='.git' \
              --exclude='.github' \
              --exclude='*.tar.gz' \
              -czf ../_release_assets/hype-qml.tar.gz .)

          # Generate checksum for QML package
          (cd _release_assets && sha256sum hype-qml.tar.gz > hype-qml.tar.gz.sha256)

          # Create full packages for each architecture
          for arch in amd64 arm64; do
            mkdir -p _temp_full/hype
            mkdir -p _temp_full/bin
            mkdir -p _temp_full/completions

            # Extract QML source
            tar -xzf _release_assets/hype-qml.tar.gz -C _temp_full/hype

            # Add CLI binaries
            if [ -f "_core_assets/hype-${arch}.gz" ]; then
              gunzip -c "_core_assets/hype-${arch}.gz" > _temp_full/bin/hype
              chmod +x _temp_full/bin/hype
            fi

            if [ -f "_core_assets/hype-distropkg-${arch}.gz" ]; then
              gunzip -c "_core_assets/hype-distropkg-${arch}.gz" > _temp_full/bin/hype-distropkg
              chmod +x _temp_full/bin/hype-distropkg
            fi

            # Add shell completions
            for completion in _core_assets/completion.*; do
              if [ -f "$completion" ]; then
                cp "$completion" _temp_full/completions/
              fi
            done

            # Copy docs directory
            if [ -d "docs" ]; then
              cp -r docs _temp_full/
            fi

            # Create installation guide
            cat > _temp_full/INSTALL.md << 'EOFINSTALL'
          # HypeMaterialShell Installation

          ## Requirements

          - Wayland compositor (niri or Hyprland recommended)
          - Quickshell framework
          - Qt6

          ## Installation Steps

          1. **Install quickshell assets:**
             ```bash
             mkdir -p ~/.config/quickshell
             cp -r hype ~/.config/quickshell/
             ```

          2. **Install the HYPE CLI binaries:**
             ```bash
             sudo install -m 755 bin/hype /usr/local/bin/hype
             ```

          3. **Install shell completions (optional):**
             ```bash
             # Bash
             sudo install -m 644 completions/completion.bash /usr/share/bash-completion/completions/hype

             # Fish
             sudo install -m 644 completions/completion.fish /usr/share/fish/vendor_completions.d/hype.fish

             # Zsh
             sudo install -m 644 completions/completion.zsh /usr/share/zsh/site-functions/_hype
             ```

          4. **Start the shell:**
             ```bash
             hype run
             ```

          ## Configuration

          - Settings are stored in `~/.config/HypeMaterialShell/settings.json`
          - Plugins go in `~/.config/HypeMaterialShell/plugins/`
          - See the documentation in the `hype/` directory for more details

          ## Troubleshooting

          - Run with verbose output: `HYPE_LOG_LEVEL=debug hype run`
          - Ensure all dependencies are installed
          EOFINSTALL

            # Create the full package
            (cd _temp_full && tar -czf "../_release_assets/hype-full-${arch}.tar.gz" .)

            # Generate checksum
            (cd _release_assets && sha256sum "hype-full-${arch}.tar.gz" > "hype-full-${arch}.tar.gz.sha256")

            # Cleanup
            rm -rf _temp_full
          done

      - name: Generate vendored source tarball
        run: |
          set -euxo pipefail
          VERSION_NUM=${TAG#v}
          cd core
          go mod vendor
          cd ..
          tar czf "_release_assets/hype-cli-${VERSION_NUM}.tar.gz" \
            --transform "s,^core/,hype-cli-${VERSION_NUM}/," \
            --exclude='core/.git' \
            core/
          (cd _release_assets && sha256sum "hype-cli-${VERSION_NUM}.tar.gz" > "hype-cli-${VERSION_NUM}.tar.gz.sha256")

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.TAG }}
          name: Release ${{ env.TAG }}
          body: ${{ steps.changelog.outputs.changelog }}
          files: _release_assets/**
          draft: false
          prerelease: ${{ contains(env.TAG, '-') }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
